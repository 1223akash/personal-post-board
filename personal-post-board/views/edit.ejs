<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-between items-center mb-4">
            <h1 class="mt-5 mb-2 text-2xl">Edit your post.</h1>
            <a class="bg-red-500 rounded-md px-3 py-1 h-8 text-sm inline-block" href="/logout">Logout</a>
        </div>
        
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="px-5 py-2 w-1/3 bg-transparent border-2 border-zinc-800 rounded-md resize-none outline-none block" placeholder="Write a post..." name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-32 text-sm text-zinc-900 bg-yellow-500 rounded-md block mt-2" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>